@model My.World.Web.ViewModel.ContinentsViewModel

@{
    ViewData["Title"] = "ViewContinents";
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Breadcrumb"] = "Continents / View";
}

<link href="~/css/quill.snow.css" rel="stylesheet" />
<link href="~/css/quill.mention.min.css" rel="stylesheet" />
<link href="~/vendors/dropzone/dropzone.css" rel="stylesheet" />
<style>
    .editorNative {
        min-height: 2em;
        max-height: 8em;
        max-width: 50em;
        margin: 0;
        margin-bottom: 2px;
        border: 1px solid #dedede;
        padding: 1px 0;
        color: black;
        font-weight: 400;
        font-size: 1.125rem;
        font-family: Arial, Helvetica, sans-serif;
        line-height: 2rem;
        background: white;
        border-bottom: 1px solid grey;
    }
</style>
<div class="c-body">
    <main class="c-main">
        <div class="container-fluid">
            <div class="fade-in">
                <div class="row">
                    <div class="col-md-12">
                        <div class="card">
                            <div class="card-header"><strong>Continent</strong> Details</div>
                            <div class="card-body">
                                <div class="row">
                                    <div class="col-md-3 col m3">
                                        <div class="card">
                                            <div class="card-header">CATEGORIES</div>
                                            <div class="card-body" style="padding:0;">
                                                <ul id="categoriesTab" class="nav flex-column nav-pills me-3 collection content-tabs" role="tablist">
                                                    @foreach (var cat in Model.ContentTemplate.categories.OrderBy(o => o.Order))
                                                    {
                                                        var active_class = cat.Order == 1 ? "active" : "";
                                                        var panelName = cat.Label + "_panel";
                                                        var tabPanelID = "tab_" + cat.Label + "_panel";
                                                        <li class="nav-item collection-item tab" id="@tabPanelID">
                                                            <a class="d-flex justify-content-between align-items-center @active_class" data-toggle="pill" href="#@panelName" role="tab" aria-controls="home">
                                                                <div>
                                                                    <span>@Html.Raw(@cat.Icon)</span>
                                                                    <span style="margin-left:20px">@cat.Label</span>
                                                                </div>
                                                                <span class="badge badge-pill badge-danger"> 0 %</span>
                                                            </a>
                                                        </li>
                                                    }
                                                </ul>
                                            </div>
                                        </div>
                                        <div class="card">
                                            <div class="card-header">REFERENCES</div>
                                            <div class="card-body" style="padding:0;">
                                                <ul id="referenceTab" class="nav flex-column nav-pills me-3 collection content-tabs" role="tablist">
                                                    @foreach (var cat in Model.ContentTemplate.references.OrderBy(o => o.Order))
                                                    {
                                                        var active_class = cat.Order == 1 ? "active" : "";
                                                        var panelName = cat.Label + "_panel";
                                                        var tabPanelID = "tab_" + cat.Label + "_panel";
                                                        <li class="nav-item collection-item tab" id="@tabPanelID">
                                                            <a class="d-flex justify-content-between align-items-center @active_class" data-toggle="pill" href="#@panelName" role="tab" aria-controls="home">
                                                                <div>
                                                                    <span>@Html.Raw(@cat.Icon)</span>
                                                                    <span style="margin-left:20px">@cat.Label</span>
                                                                </div>
                                                                <span class="badge badge-pill badge-danger"> 0 %</span>
                                                            </a>
                                                        </li>
                                                    }
                                                </ul>
                                            </div>
                                        </div>
                                    </div>
                                    <div class="col-md-8">
                                        <div class="align-items-start">
                                            <div id="tabContent" class="tab-content">
                                                @foreach (var cat in Model.ContentTemplate.categories)
                                                {
                                                    var active_class = cat.Order == 1 ? "active" : "";
                                                    var panelName = cat.Label + "_panel";

                                                    <div class="tab-pane @active_class" id="@panelName" role="tabpanel">
                                                        <div class="card">
                                                            <div class="card-header d-flex justify-content-between align-items-center" style="background:@Model.headerBackgroundColor">
                                                                <span>@cat.Label</span>
                                                                <span>@Html.Raw(@cat.Icon)</span>
                                                            </div>
                                                            <div class="card-body" style="padding:10px;">
                                                                @foreach (var att in cat.Attributes)
                                                                {
                                                                    if (att.is_active)
                                                                    {
                                                                    <div class="form-group row">
                                                                        <label class="col-md-3 col-form-label" for="text-input">@att.field_label</label>
                                                                        <div class="col-md-9">
                                                                            @if (att.field_name.ToLower() != "name" && att.field_type.In(new List<string>() { "binary", "varbinary", "tinyblob", "blob", "mediumblob", "longblob", "char byte", "char", "varchar", "tinytext", "text", "mediumtext", "longtext", "set", "enum", "nchar", "national char", "nvarchar", "national varchar", "character varying" }))
                                                                                {
                                                                                <div id="@att.field_name" class="form-control editorNative"></div>
                                                                                }
                                                                                else if (att.field_name.ToLower() == "universe")
                                                                                {
                                                                                <select id="ddlUniverseList" class="form-control" style="min-width:150px"
                                                                                        asp-items="@(new SelectList(@Model.UniversesList, " id", "name" , Model.continentsModel.Universe))"
                                                                                        onchange="UniverseSelected();">
                                                                                </select>
                                                                                }
                                                                                else if (att.field_type.In(new List<string>
                                                                                    () { "int", "integer", "smallint unsigned", "mediumint", "bigint", "int unsigned", "integer unsigned", "bit" }))
                                                                                    {
                                                                                    <input id="continentsModel_@att.field_name" class="form-control" type="number" />
                                                                                    }
                                                                                    else if (att.field_type.In(new List<string>
                                                                                        () { "tinyint" }))
                                                                                        {
                                                                                        <input id="continentsModel_@att.field_name" class="form-control" type="checkbox" />
                                                                                        }
                                                                                        else
                                                                                        {
                                                                                        <input id="continentsModel_@att.field_name" class="form-control" />

                                                                                        }
                                                                        </div>
                                                                    </div>
                                                                    }
                                                                }
                                                            </div>
                                                        </div>
                                                    </div>
                                                }


                                                @foreach (var cat in Model.ContentTemplate.references)
                                                {
                                                    var active_class = cat.Order == 1 ? "active" : "";
                                                    var panelName = cat.Label + "_panel";

                                                    <div class="tab-pane @active_class" id="@panelName" role="tabpanel">
                                                        <div class="card">
                                                            <div class="card-header d-flex justify-content-between align-items-center" style="background:cadetblue">
                                                                <span>@cat.Label</span>
                                                                <span>@Html.Raw(@cat.Icon)</span>
                                                            </div>
                                                            <div class="card-body" style="padding:10px;">
                                                                @foreach (var att in cat.Attributes)
                                                                {
                                                                    if (att.is_active)
                                                                    {
                                                                        if (att.field_name.ToLower() == "gallery")
                                                                        {
                                                                            <div class="mb-3">
                                                                                <label class="form-label">Uploaded Images</label>

                                                                                <div class="row">
                                                                                    @foreach (var contentObject in Model.ContentObjectModelList)
                                                                                    {
                                                                                        <div class="card-img" style="width: 18rem;margin-right: 1.5rem;">
                                                                                            <div class="card-body img-container" style="height: 265px;background: url('@contentObject.file_url');background-size: cover;">
                                                                                                <a class="remove-image" style="display: inline;"
                                                                                                   asp-controller="buildings" asp-action="DeleteAttachment" asp-route-objectId="@contentObject.object_id" asp-route-objectName="@contentObject.object_name">&#215;</a>
                                                                                            </div>
                                                                                        </div>
                                                                                    }
                                                                                </div>
                                                                            </div>

                                                                            <div class="mb-3">
                                                                                <label class="form-label" for="exampleFormControlInput1">Upload Images...</label>
                                                                                <div>
                                                                                    @using (Html.BeginForm("UploadAttachment", "buildings", FormMethod.Post, new { @class = "", @name = "myDropzone", id = "myDropzone", @enctype = "multipart/form-data" }))
                                                                                    {
                                                                                    <div class="dz-message needsclick">
                                                                                        <button type="button" class="dz-button">Drop files here or click to upload.</button><br />
                                                                                        <span class="note needsclick"></span>
                                                                                    </div>
                                                                                    <div class="fallback">
                                                                                        <input name="postedfiles" type="file" multiple />
                                                                                    </div>
                                                                                    <div>
                                                                                        @*<input type="submit" id="Submit" name="Submit" class="btn btn-success m-t-5" value="Submit" />*@
                                                                                    </div>
                                                                                    }

                                                                                    <a href="#" id="saveAttchbtn" name="saveAttchbtn" class="btn btn-primary">Upload</a>
                                                                                </div>
                                                                            </div>
                                                                        }
                                                                    }

                                                                }
                                                            </div>
                                                        </div>
                                                    </div>
                                                }
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="card-footer">
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>
</div>

<div id="preview-template" style="display: none;">
    <div class="dz-preview dz-file-preview">
        <div class="dz-image">
            <img data-dz-thumbnail />
        </div>
        <div class="dz-details">
            <div class="dz-size">
                <span data-dz-size />
            </div>
            <div class="dz-filename">
                <span data-dz-name />
            </div>
        </div>
        <div class="dz-progress">
            <span class="dz-upload" data-dz-uploadprogress />
        </div>
        <div class="dz-error-message">
            <span data-dz-errormessage />
        </div>
        <div class="dz-success-mark">
            <svg width="54px" height="54px" viewBox="0 0 54 54" version="1.1" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink"
                 xmlns:sketch="http://www.bohemiancoding.com/sketch/ns">
                <!-- Generator: Sketch 3.2.1 (9971) - http://www.bohemiancoding.com/sketch -->
                <title>Check</title>
                <desc>Created with Sketch.</desc>
                <defs />
                <g id="Page-1" stroke="none" stroke-width="1" fill="none" fill-rule="evenodd" sketch:type="MSPage">
                    <path d="M23.5,31.8431458 L17.5852419,25.9283877 C16.0248253,24.3679711 13.4910294,24.366835 11.9289322,25.9289322 C10.3700136,27.4878508 10.3665912,30.0234455 11.9283877,31.5852419 L20.4147581,40.0716123 C20.5133999,40.1702541 20.6159315,40.2626649 20.7218615,40.3488435 C22.2835669,41.8725651 24.794234,41.8626202 26.3461564,40.3106978 L43.3106978,23.3461564 C44.8771021,21.7797521 44.8758057,19.2483887 43.3137085,17.6862915 C41.7547899,16.1273729 39.2176035,16.1255422 37.6538436,17.6893022 L23.5,31.8431458 Z M27,53 C41.3594035,53 53,41.3594035 53,27 C53,12.6405965 41.3594035,1 27,1 C12.6405965,1 1,12.6405965 1,27 C1,41.3594035 12.6405965,53 27,53 Z" id="Oval-2" stroke-opacity="0.198794158" stroke="#747474" fill-opacity="0.816519475" fill="#FFFFFF" sketch:type="MSShapeGroup" />
                </g>
            </svg>
        </div>
        <div class="dz-error-mark">
            <svg width="54px" height="54px" viewBox="0 0 54 54" version="1.1" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink"
                 xmlns:sketch="http://www.bohemiancoding.com/sketch/ns">
                <!-- Generator: Sketch 3.2.1 (9971) - http://www.bohemiancoding.com/sketch -->
                <title>error</title>
                <desc>Created with Sketch.</desc>
                <defs />
                <g id="Page-1" stroke="none" stroke-width="1" fill="none" fill-rule="evenodd" sketch:type="MSPage">
                    <g id="Check-+-Oval-2" sketch:type="MSLayerGroup" stroke="#747474" stroke-opacity="0.198794158" fill="#FFFFFF" fill-opacity="0.816519475">
                        <path d="M32.6568542,29 L38.3106978,23.3461564 C39.8771021,21.7797521 39.8758057,19.2483887 38.3137085,17.6862915 C36.7547899,16.1273729 34.2176035,16.1255422 32.6538436,17.6893022 L27,23.3431458 L21.3461564,17.6893022 C19.7823965,16.1255422 17.2452101,16.1273729 15.6862915,17.6862915 C14.1241943,19.2483887 14.1228979,21.7797521 15.6893022,23.3461564 L21.3431458,29 L15.6893022,34.6538436 C14.1228979,36.2202479 14.1241943,38.7516113 15.6862915,40.3137085 C17.2452101,41.8726271 19.7823965,41.8744578 21.3461564,40.3106978 L27,34.6568542 L32.6538436,40.3106978 C34.2176035,41.8744578 36.7547899,41.8726271 38.3137085,40.3137085 C39.8758057,38.7516113 39.8771021,36.2202479 38.3106978,34.6538436 L32.6568542,29 Z M27,53 C41.3594035,53 53,41.3594035 53,27 C53,12.6405965 41.3594035,1 27,1 C12.6405965,1 1,12.6405965 1,27 C1,41.3594035 12.6405965,53 27,53 Z" id="Oval-2" sketch:type="MSShapeGroup" />
                    </g>
                </g>
            </svg>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/js/quill.js"></script>
    <script src="~/js/quill.mention.min.js"></script>
    <script src="~/js/ContinentsEditor.js"></script>
    <script src="~/js/common.js"></script>
    <script src="~/vendors/dropzone/dropzone.js"></script>

    <script type="text/javascript">

        			function setArchitectureBody() {
			    Architecture_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Architecture'));
			    $('#Architecture').focusout(function () {
			        postSaveEditorData('Architecture', Architecture_editor.root.innerHTML);
			    });
			};

			function setAreaBody() {
			    $('#continentsModel_Area').val('@Model.continentsModel.Area');
			};

			function setBodies_of_waterBody() {
			    Bodies_of_water_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Bodies_of_water'));
			    $('#Bodies_of_water').focusout(function () {
			        postSaveEditorData('Bodies_of_water', Bodies_of_water_editor.root.innerHTML);
			    });
			};

			function setCountriesBody() {
			    Countries_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Countries'));
			    $('#Countries').focusout(function () {
			        postSaveEditorData('Countries', Countries_editor.root.innerHTML);
			    });
			};

			function setCreaturesBody() {
			    Creatures_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Creatures'));
			    $('#Creatures').focusout(function () {
			        postSaveEditorData('Creatures', Creatures_editor.root.innerHTML);
			    });
			};

			function setCropsBody() {
			    Crops_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Crops'));
			    $('#Crops').focusout(function () {
			        postSaveEditorData('Crops', Crops_editor.root.innerHTML);
			    });
			};

			function setDemonymBody() {
			    Demonym_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Demonym'));
			    $('#Demonym').focusout(function () {
			        postSaveEditorData('Demonym', Demonym_editor.root.innerHTML);
			    });
			};

			function setDescriptionBody() {
			    Description_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Description'));
			    $('#Description').focusout(function () {
			        postSaveEditorData('Description', Description_editor.root.innerHTML);
			    });
			};

			function setDiscoveryBody() {
			    Discovery_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Discovery'));
			    $('#Discovery').focusout(function () {
			        postSaveEditorData('Discovery', Discovery_editor.root.innerHTML);
			    });
			};

			function setEconomyBody() {
			    Economy_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Economy'));
			    $('#Economy').focusout(function () {
			        postSaveEditorData('Economy', Economy_editor.root.innerHTML);
			    });
			};

			function setFlorasBody() {
			    Floras_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Floras'));
			    $('#Floras').focusout(function () {
			        postSaveEditorData('Floras', Floras_editor.root.innerHTML);
			    });
			};

			function setFormationBody() {
			    Formation_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Formation'));
			    $('#Formation').focusout(function () {
			        postSaveEditorData('Formation', Formation_editor.root.innerHTML);
			    });
			};

			function setGovernmentsBody() {
			    Governments_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Governments'));
			    $('#Governments').focusout(function () {
			        postSaveEditorData('Governments', Governments_editor.root.innerHTML);
			    });
			};

			function setHumidityBody() {
			    Humidity_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Humidity'));
			    $('#Humidity').focusout(function () {
			        postSaveEditorData('Humidity', Humidity_editor.root.innerHTML);
			    });
			};

			function setLandmarksBody() {
			    Landmarks_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Landmarks'));
			    $('#Landmarks').focusout(function () {
			        postSaveEditorData('Landmarks', Landmarks_editor.root.innerHTML);
			    });
			};

			function setLanguagesBody() {
			    Languages_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Languages'));
			    $('#Languages').focusout(function () {
			        postSaveEditorData('Languages', Languages_editor.root.innerHTML);
			    });
			};

			function setLocal_nameBody() {
			    Local_name_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Local_name'));
			    $('#Local_name').focusout(function () {
			        postSaveEditorData('Local_name', Local_name_editor.root.innerHTML);
			    });
			};

			function setMineralogyBody() {
			    Mineralogy_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Mineralogy'));
			    $('#Mineralogy').focusout(function () {
			        postSaveEditorData('Mineralogy', Mineralogy_editor.root.innerHTML);
			    });
			};

			function setNatural_disastersBody() {
			    Natural_disasters_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Natural_disasters'));
			    $('#Natural_disasters').focusout(function () {
			        postSaveEditorData('Natural_disasters', Natural_disasters_editor.root.innerHTML);
			    });
			};

			function setNotesBody() {
			    Notes_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Notes'));
			    $('#Notes').focusout(function () {
			        postSaveEditorData('Notes', Notes_editor.root.innerHTML);
			    });
			};

			function setOther_NamesBody() {
			    Other_Names_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Other_Names'));
			    $('#Other_Names').focusout(function () {
			        postSaveEditorData('Other_Names', Other_Names_editor.root.innerHTML);
			    });
			};

			function setPoliticsBody() {
			    Politics_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Politics'));
			    $('#Politics').focusout(function () {
			        postSaveEditorData('Politics', Politics_editor.root.innerHTML);
			    });
			};

			function setPopular_foodsBody() {
			    Popular_foods_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Popular_foods'));
			    $('#Popular_foods').focusout(function () {
			        postSaveEditorData('Popular_foods', Popular_foods_editor.root.innerHTML);
			    });
			};

			function setPopulationBody() {
			    Population_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Population'));
			    $('#Population').focusout(function () {
			        postSaveEditorData('Population', Population_editor.root.innerHTML);
			    });
			};

			function setPrecipitationBody() {
			    Precipitation_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Precipitation'));
			    $('#Precipitation').focusout(function () {
			        postSaveEditorData('Precipitation', Precipitation_editor.root.innerHTML);
			    });
			};

			function setPrivate_NotesBody() {
			    Private_Notes_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Private_Notes'));
			    $('#Private_Notes').focusout(function () {
			        postSaveEditorData('Private_Notes', Private_Notes_editor.root.innerHTML);
			    });
			};

			function setRegional_advantagesBody() {
			    Regional_advantages_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Regional_advantages'));
			    $('#Regional_advantages').focusout(function () {
			        postSaveEditorData('Regional_advantages', Regional_advantages_editor.root.innerHTML);
			    });
			};

			function setRegional_disadvantagesBody() {
			    Regional_disadvantages_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Regional_disadvantages'));
			    $('#Regional_disadvantages').focusout(function () {
			        postSaveEditorData('Regional_disadvantages', Regional_disadvantages_editor.root.innerHTML);
			    });
			};

			function setReputationBody() {
			    Reputation_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Reputation'));
			    $('#Reputation').focusout(function () {
			        postSaveEditorData('Reputation', Reputation_editor.root.innerHTML);
			    });
			};

			function setRuinsBody() {
			    Ruins_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Ruins'));
			    $('#Ruins').focusout(function () {
			        postSaveEditorData('Ruins', Ruins_editor.root.innerHTML);
			    });
			};

			function setSeasonsBody() {
			    Seasons_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Seasons'));
			    $('#Seasons').focusout(function () {
			        postSaveEditorData('Seasons', Seasons_editor.root.innerHTML);
			    });
			};

			function setShapeBody() {
			    Shape_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Shape'));
			    $('#Shape').focusout(function () {
			        postSaveEditorData('Shape', Shape_editor.root.innerHTML);
			    });
			};

			function setTagsBody() {
			    Tags_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Tags'));
			    $('#Tags').focusout(function () {
			        postSaveEditorData('Tags', Tags_editor.root.innerHTML);
			    });
			};

			function setTemperatureBody() {
			    Temperature_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Temperature'));
			    $('#Temperature').focusout(function () {
			        postSaveEditorData('Temperature', Temperature_editor.root.innerHTML);
			    });
			};

			function setTopographyBody() {
			    Topography_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Topography'));
			    $('#Topography').focusout(function () {
			        postSaveEditorData('Topography', Topography_editor.root.innerHTML);
			    });
			};

			function setTourismBody() {
			    Tourism_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Tourism'));
			    $('#Tourism').focusout(function () {
			        postSaveEditorData('Tourism', Tourism_editor.root.innerHTML);
			    });
			};

			function setTraditionsBody() {
			    Traditions_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Traditions'));
			    $('#Traditions').focusout(function () {
			        postSaveEditorData('Traditions', Traditions_editor.root.innerHTML);
			    });
			};

			function setUniverseBody() {
			    $('#continentsModel_Universe').val('@Model.continentsModel.Universe');
			};

			function setWarsBody() {
			    Wars_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Wars'));
			    $('#Wars').focusout(function () {
			        postSaveEditorData('Wars', Wars_editor.root.innerHTML);
			    });
			};

			function setWindsBody() {
			    Winds_editor.clipboard.dangerouslyPasteHTML(htmlDecode('@Model.continentsModel.Winds'));
			    $('#Winds').focusout(function () {
			        postSaveEditorData('Winds', Winds_editor.root.innerHTML);
			    });
			};



        $(document).ready(function () {
            			$('#continentsModel_Area').blur(function() {
			    postSaveData('Area', $('#continentsModel_Area').val());
			});



            Dropzone.autoDiscover = false;
            Dropzone.options.myDropzone = false;

            $('#myDropzone').dropzone({
                previewTemplate: document.querySelector('#preview-template').innerHTML,
                paramName: "files",
                autoProcessQueue: false,
                uploadMultiple: true,
                parallelUploads: 100,
                maxFiles: 100,
                maxFilesize: 100,//max file size in MB,
                addRemoveLinks: true,
                dictResponseError: 'Server not Configured',
                thumbnail: function (file, dataUrl) {
                    if (file.previewElement) {
                        file.previewElement.classList.remove("dz-file-preview");
                        var images = file.previewElement.querySelectorAll("[data-dz-thumbnail]");
                        for (var i = 0; i < images.length; i++) {
                            var thumbnailElement = images[i];
                            thumbnailElement.alt = file.name;
                            thumbnailElement.src = dataUrl;
                        }
                        setTimeout(function () { file.previewElement.classList.add("dz-image-preview"); }, 1);
                    }
                },
                acceptedFiles: ".png,.jpg,.gif,.bmp,.jpeg,.pdf",// use this to restrict file type
                init: function () {
                    var self = this;
                    // config
                    self.options.addRemoveLinks = true;
                    self.options.dictRemoveFile = "Delete";
                    //New file added
                    self.on("addedfile", function (file) {
                        console.log('new file added ', file);
                        $('.dz-success-mark').hide();
                        $('.dz-error-mark').hide();
                    });
                    // Send file starts
                    self.on("sending", function (file) {
                        console.log('upload started', file);
                        $('.meter').show();
                    });

                    // File upload Progress
                    self.on("totaluploadprogress", function (progress) {
                        console.log("progress ", progress);
                        $('.roller').width(progress + '%');
                    });

                    self.on("queuecomplete", function (progress) {
                        $('.meter').delay(999).slideUp(999);
                        console.log("Files uploaded");
                        location.reload();
                    });
                    self.on("complete", function (file) {
                        self.removeFile(file);
                    });
                    // On removing file
                    self.on("removedfile", function (file) {
                        console.log(file);
                    });

                    $('#saveAttchbtn').on("click", function (e) {
                        e.preventDefault();
                        e.stopPropagation();
                        // Validate form here if needed

                        if (self.getQueuedFiles().length > 0) {
                            self.processQueue();

                        } else {
                            self.uploadFiles([]);
                            $('#myDropzone').submit();
                        }

                    });

                    self.on("successmultiple", function (files, response) {
                        // Gets triggered when the files have successfully been sent.
                        // Redirect user or notify of success.

                    });
                }
            });

            $('#myDropzone').addClass("dropzone");

            if (window.location.hash) {
                var idPrefix = "tab_";
                var panelName = window.location.hash.replace('#', '');
                var liElement = $('#' + idPrefix + panelName);
                var tabContentEle = $('#' + panelName);

                if (liElement != undefined) {
                    $('#tabContent').find('div').removeClass('active');
                    $('#categoriesTab').find('li').find('a').removeClass('active');
                    $('#referenceTab').find('li').find('a').removeClass('active');
                    liElement.find('a').addClass('active');
                    tabContentEle.addClass('active');
                    console.log(liElement);
                }
            }
        });

        function postSaveData(type, value) {
            var url = '/Continents/Save' + type;

            $.ajax({
                type: 'POST',
                url: url,
                data: '{ ContinentID : "' + @ViewData["ContinentID"] + '", value : "' + value + '" }',
                    dataType: "json",
                        contentType: "application/json; charset=utf-8",
                            success: function (result) {
                                if (result.isSuccess) {

                                }
                                else {
                                    $.alert({
                                        title: 'Error',
                                        icon: 'fa fa-warning',
                                        type: 'red',
                                        content: '<hr>' + result.message + '</hr>',
                                    });
                                }
                            },
            error: function (error) {
                $.alert({
                    title: 'Error',
                    icon: 'fa fa-warning',
                    type: 'red',
                    content: '<hr>' + error + '</hr>',
                });
            }
        });
        }


        function postSaveEditorData(type, value) {
            var url = '/Continents/' + @ViewData["ContinentID"] + '/Save' + type;

            $.ajax({
                type: 'POST',
                url: url,
                data: value,
                async: false,
                contentType: "application/json; charset=utf-8",
                success: function (result) {
                    if (result.isSuccess) {

                    }
                    else {
                        $.alert({
                            title: 'Error',
                            icon: 'fa fa-warning',
                            type: 'red',
                            content: '<hr>' + result.message + '</hr>',
                        });
                    }
                },
                error: function (error) {
                    $.alert({
                        title: 'Error',
                        icon: 'fa fa-warning',
                        type: 'red',
                        content: '<hr>' + error + '</hr>',
                    });
                }
            });
        }

    </script>
}